Class extends Response

Class constructor
	C_VARIANT:C1683($1)
	C_LONGINT:C283($2)
	C_OBJECT:C1216($3)
	ASSERT:C1129(OB Instance of:C1731($1;4D:C1709.File))  // only file, if we want path we must provide default root folder path
	Super:C1705($1;$2;$3)
	